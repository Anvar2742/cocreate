export interface CourseDoc {
    title: string;
    description: string;
    _id: string;
    slug: string;
}
export interface LessonDoc {
    title: string;
    description: string;
    _id: string;
    slug: string;
    content: string;
}

import AuthModal from "../components/AuthModal";

const Auth = () => {
    return (
        <AuthModal
            toggleAuthModal={null}
            isAuthModal={null}
            isAuthPage={true}
        />
    );
};

export default Auth;
